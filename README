# Art Digitizer

### Digitize Drawings
Run the following command to digitize a drawing:
`python digitize.py <path-to-image> <output-path>`

Flags:
- `--background`: Use a white background (default: transparent)
- `--constant`: The constant value for adaptive thresholding (default: 20)
- `--binary`: No semi-transparent pixels
- `--color`: Render the image in color
